module.exports = {
  name: "lockdown",
  description: "Lock the current channel.",
  usage: "[Time]",
  aliases: ["lock"],
  category: "Moderation",
  execute(message, args, client, Discord, dbl, mongoose, Schemas) {
    // if(!args[1]) return message.reply("Not a valid amount of **test tubes**.")
    // var amount = args[1].toLowerCase() == 'all' ? 
    // if(isNaN(amount)) return message.reply("Not a valid amount of **test tubes**.")
  }  
}